machine:
  environment:
    TF_TEST_WITH_POSTGRES: true
database:
  override:
    - createdb pred
    - createuser -s -w pred_user
test:
  override:
    - nosetests # test python code
    - ./testjs.sh #test javascript
